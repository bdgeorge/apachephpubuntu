#!/bin/sh

# Upload the dev database if we can find it and git
# $(which mysql > /dev/null 2>&1)
# FOUND_MYSQL=$?
# $(ls /vagrant/database/devddd.sql > /dev/null 2>&1)
# FOUND_DEV_SQL=$?
# #echo "$FOUND_MYSQL"
# #echo "$FOUND_DEV_SQL"
# if [ "$FOUND_MYSQL" -ne '0' ]; then
#   echo 'Unable to find MySQL'
# else
#   echo 'Uploading dev database'
#   mysql -u dev -pdev dev < /vagrant/database/dev.sql
# fi


